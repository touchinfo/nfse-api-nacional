# Banco de dados
DB_HOST=localhost
DB_PORT=3306
DB_USER=seu_usuario
DB_PASSWORD=sua_senha
DB_NAME=nfse_nacional

# Chave de criptografia (gere uma nova!)
# Execute: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=sua_chave_de_32_bytes_aqui

# Servidor
PORT=3000
NODE_ENV=production